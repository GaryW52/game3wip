Button button1;
Keybinds keybind;

void setup() {
  // Create variables for resolution in config class
  size(Config.screenWidth, Config.screenHeight);
  button1 = new Button(150, 150, 80, 80);
}

void draw() {
  //TODO pull start screen class then display it
  
  //TODO pull main menu class then display
}
